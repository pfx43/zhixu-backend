# 知拾 (Zhishi) 后端 API 接口功能清单

> **版本**：v3.1（基于 TCN 源码确认后修订）
> **日期**：2026-07-15
> **基线代码**：`zhishi-master/backend`（经 2026-07-15 代码审计）
> **治理依据**：`zhixu-v4.md` v2.0（TCN 对接与重复接口治理方案，源码级确认）
> **TCN 对接**：`SYSEM_ARCHITECTURE_BRIEF.md` + `TCN_API_CONFIRMATION_REPLY.md`
> **说明**：本文档整合已实现接口 + TCN 对接改造变更 + 待实现接口。所有 TCN 相关字段均经 TCN 方源码级核实。

---

## 目录

- [通用说明](#通用说明)
- [1. 账号认证 (Auth)](#1-账号认证-auth)
- [2. 用户套餐 (Plan)](#2-用户套餐-plan)
- [3. 智能聊天 (Chat)](#3-智能聊天-chat)
- [4. 知识库管理 (KB)](#4-知识库管理-kb)
- [5. 知识追踪 (KT)](#5-知识追踪-kt)
- [6. 题目管理 (Questions)](#6-题目管理-questions)
- [7. 刷题 (Quiz)](#7-刷题-quiz)
- [8. 辅导 (Tutor)](#8-辅导-tutor)
- [9. 首页建议 (Dashboard)](#9-首页建议-dashboard)
- [10. 学习分析 (Analytics)](#10-学习分析-analytics)
- [11. 学习报告 (Reports)](#11-学习报告-reports)
- [12. 针对训练 (Training)](#12-针对训练-training)
- [13. 个人画像 (Profile)](#13-个人画像-profile)
- [14. 笔记系统 (Notes)](#14-笔记系统-notes)
- [15. 提醒系统 (Reminders)](#15-提醒系统-reminders)
- [16. 通知中心 (Notifications)](#16-通知中心-notifications)
- [17. 统一搜索 (Search)](#17-统一搜索-search)
- [18. 学习路径 (Learning Path)](#18-学习路径-learning-path)
- [19. 数据同步 (Sync)](#19-数据同步-sync)
- [20. 系统健康 (Health)](#20-系统健康-health)
- [附录 A：状态码规范](#附录-a状态码规范)
- [附录 B：TCN 外部接口参考](#附录-btcn-外部接口参考)

---

## 通用说明

### 鉴权方式

所有业务接口（除注册、登录、密码重置、健康检查外）需在请求头中携带 Bearer Token：

```
Authorization: Bearer <access_token>
```

### 基础路径

```
http://<host>:8765/api/v1
```

### 通用错误响应格式

```json
{
  "detail": "错误描述信息"
}
```

### 接口状态标记

| 标记 | 含义 |
|------|------|
| ✅ 已实现 | 代码已完成，可直接调用 |
| 🔄 已改造 | TCN 对接后数据源已切换（接口路径不变，响应结构前向兼容） |
| ❌ 已淘汰 | TCN 对接后删除（返回 404） |
| 📋 待实现 | 规划中，尚未开发 |
| ➕ v2.0 新增 | TCN 对接后新增接口 |

---

## 1. 账号认证 (Auth)

**路由前缀**：`/api/v1/auth`
**权限**：部分接口免登录

### 1.1 接口列表

| 方法 | 路径 | 鉴权 | 状态 | 说明 |
|------|------|------|------|------|
| POST | `/auth/register` | 否 | ✅ | 用户注册 |
| POST | `/auth/token` | 否 | ✅ | 用户登录（OAuth2 password grant） |
| POST | `/auth/refresh-token` | 是 | ✅ | 刷新 Token |
| GET | `/auth/users/me` | 是 | ✅ | 获取当前用户信息（含套餐详情） |
| GET | `/auth/users/me/plan` | 是 | ✅ | 获取当前用户套餐详情 |
| GET | `/auth/users/me/quota` | 是 | ✅ | 检查 API 配额状态 |
| POST | `/auth/users/me/upgrade-plan` | 是 | ✅ | 升级套餐 |
| PATCH | `/auth/users/me` | 是 | ✅ | 更新个人资料（白名单字段） |
| POST | `/auth/send-verification` | 否 | ✅ | 发送验证码（邮箱或手机号） |
| POST | `/auth/verify-email` | 否 | ✅ | 验证邮箱 |
| POST | `/auth/forgot-password` | 否 | ✅ | 发送密码重置邮件 |
| POST | `/auth/reset-password` | 否 | ✅ | 使用重置 Token 修改密码 |
| POST | `/auth/change-password` | 是 | ✅ | 已登录用户修改密码 |
| POST | `/auth/logout` | 是 | ✅ | 退出登录 |
| DELETE | `/auth/account` | 是 | ✅ | 注销账号 |
| GET | `/auth/check-email-verification/{email}` | 否 | ✅ | 检查邮箱验证状态 |
| GET | `/auth/test-token-info` | 是 | ✅ | 测试 Token 有效性 |

### 1.2 关键接口详情

#### 注册

```
POST /api/v1/auth/register
```

**请求体**：
```json
{
  "email": "user@example.com",
  "password": "secure_password",
  "username": "可选用户名",
  "nickname": "可选昵称",
  "verification_code": "可选验证码"
}
```

**响应**（201）：
```json
{
  "id": 1,
  "email": "user@example.com",
  "username": null,
  "nickname": "新用户",
  "is_active": true,
  "created_at": "2026-07-15T00:00:00Z",
  "access_token": "eyJ...",
  "token_type": "bearer",
  "message": "注册成功"
}
```

#### 登录

```
POST /api/v1/auth/token
Content-Type: application/x-www-form-urlencoded
```

**请求体**：
```
username=user@example.com（或手机号）
password=secure_password
```

**响应**：
```json
{
  "access_token": "eyJ...",
  "token_type": "bearer",
  "expires_in": 604800
}
```

#### 获取当前用户信息

```
GET /api/v1/auth/users/me
Authorization: Bearer <token>
```

**响应**：
```json
{
  "id": 1,
  "email": "user@example.com",
  "phone": "13800138000",
  "nickname": "张三",
  "gender": "男",
  "signature": "终身学习者",
  "tags": "[\"程序员\",\"学生\"]",
  "username": "zhangsan",
  "is_active": true,
  "created_at": "2026-01-01T00:00:00Z",
  "plan_info": {
    "level": 0,
    "name": "免费版",
    "daily_api_limit": 10,
    "monthly_token_limit": 10000,
    "kb_limit": 1,
    "available_models": ["gpt-3.5-turbo"],
    "concurrent_limit": 1,
    "expires_at": null,
    "days_remaining": null
  }
}
```

#### 更新个人资料

```
PATCH /api/v1/auth/users/me
Authorization: Bearer <token>
```

**请求体**（白名单字段：phone / nickname / gender / signature / tags）：
```json
{
  "nickname": "新昵称",
  "signature": "新签名"
}
```

**响应**：
```json
{
  "message": "资料更新成功",
  "updated_fields": ["nickname", "signature"]
}
```

---

## 2. 用户套餐 (Plan)

**路由前缀**：`/api/v1/plan`

| 方法 | 路径 | 鉴权 | 状态 | 说明 |
|------|------|------|------|------|
| GET | `/plan/` | 否 | ✅ | 获取所有可用套餐列表 |
| GET | `/plan/my-plan` | 是 | ✅ | 获取当前用户套餐及升级选项 |

**所有套餐响应**：
```json
[
  {
    "level": 0,
    "name": "免费版",
    "price_monthly": 0.0,
    "api_limit_daily": 10,
    "token_limit_monthly": 10000,
    "knowledge_base_limit": 1,
    "model_access": "gpt-3.5-turbo",
    "concurrent_limit": 1
  },
  {
    "level": 1,
    "name": "标准版",
    "price_monthly": 29.9,
    "api_limit_daily": 100,
    "token_limit_monthly": 100000,
    "knowledge_base_limit": 10,
    "model_access": "gpt-4o-mini",
    "concurrent_limit": 5
  }
]
```

---

## 3. 智能聊天 (Chat)

**路由前缀**：`/api/v1/chat`
**鉴权**：是
**存储**：Redis 缓存 + 文件持久化（storage/）

### 3.1 接口列表

| 方法 | 路径 | 状态 | 说明 |
|------|------|------|------|
| POST | `/chat` | ✅ | 发送消息（stream=true 时 SSE 流式） |
| GET | `/chat/sessions` | ✅ | 列出当前用户所有会话 |
| GET | `/chat/history?session_id=xxx` | ✅ | 获取指定会话聊天历史 |
| DELETE | `/chat/sessions/{session_id}` | ✅ | 删除指定会话 |

### 3.2 发送消息

```
POST /api/v1/chat
Authorization: Bearer <token>
```

**请求体**：
```json
{
  "content": "请解释微积分的基本原理",
  "session_id": "a1b2c3d4e5f6（可选，不传则自动创建新会话）",
  "stream": true,
  "collection_id": "可选知识库分区ID，限定检索范围"
}
```

**非流式响应**（200）：
```json
{
  "session_id": "a1b2c3d4e5f6",
  "session_title": "请解释微积分的基本原理",
  "role": "assistant",
  "content": "微积分是研究函数变化率和累积量的数学分支...",
  "created_at": "2026-07-15T10:00:00Z",
  "citations": [
    {
      "document_id": "doc_001",
      "document_name": "高等数学.pdf",
      "segment_index": 3,
      "content_snippet": "微积分的基本思想是...",
      "similarity": 0.92
    }
  ]
}
```

**SSE 流式事件格式**（stream=true）：
```
event: message
data: {"session_id":"a1b2","role":"assistant","content":"微"}

event: message
data: {"session_id":"a1b2","role":"assistant","content":"积分","reasoning_content":"先考虑函数的极限..."}

event: message
data: {"session_id":"a1b2","role":"assistant","content":"是...","citations":[...],"diagnosis":"LVR=0.12...","lvr":0.12}
```

**SSE 数据字段**：

| 字段 | 类型 | 说明 |
|------|------|------|
| `session_id` | string | 会话 ID |
| `role` | string | 消息角色（assistant / tool / system） |
| `content` | string | 回复文本片段 |
| `reasoning_content` | string? | 推理过程片段（推理类 LLM 输出） |
| `tool_name` | string? | 工具调用名称 |
| `citations` | array? | 引用信息（含 document_id、document_name、segment_index、content_snippet、similarity） |
| `lvr` | float? | TCN 全局 LVR（知识状态一致性指标，0–1）— TCN 对接后新增 |
| `diagnosis` | string? | TCN 诊断描述— TCN 对接后新增 |

---

## 4. 知识库管理 (KB)

**路由前缀**：`/api/v1/kb`
**鉴权**：是

### 4.1 知识库分区 (Collections)

| 方法 | 路径 | 状态 | 说明 |
|------|------|------|------|
| GET | `/kb/collections` | ✅ | 列出所有分区 |
| POST | `/kb/collections` | ✅ | 创建分区 |
| PATCH | `/kb/collections/{collection_id}` | ✅ | 更新分区名称/描述 |
| DELETE | `/kb/collections/{collection_id}` | ✅ | 删除分区 |
| GET | `/kb/categories` | ✅ | 分区别名（兼容前端路径） |
| POST | `/kb/categories` | ✅ | 创建分区（别名） |
| PATCH | `/kb/categories/{collection_id}` | ✅ | 更新分区（别名） |
| DELETE | `/kb/categories/{collection_id}` | ✅ | 删除分区（别名） |

**创建分区请求**：
```json
{
  "name": "数学笔记",
  "zone": "study",
  "description": "高等数学相关资料"
}
```

> `zone` 枚举：`"study"`（学习区） / `"life"`（生活区）

**分区列表响应**：
```json
{
  "collections": [
    {
      "id": "col_xxx",
      "name": "数学笔记",
      "zone": "study",
      "description": "高等数学相关资料",
      "dataset_id": "ds_xxx",
      "is_default": false,
      "created_at": "2026-07-15T00:00:00Z",
      "updated_at": "2026-07-15T10:00:00Z"
    }
  ],
  "total": 1
}
```

### 4.2 文档管理

| 方法 | 路径 | 状态 | 说明 |
|------|------|------|------|
| POST | `/kb/upload` | ✅ | 上传文档（multipart/form-data） |
| GET | `/kb/documents` | ✅ | 文档列表（分页，可按 collection_id 过滤） |
| GET | `/kb/documents/{batch_id}/status` | ✅ | 查询索引进度 |
| GET | `/kb/documents/{doc_id}/content` | ✅ | 获取解析后文本内容 |
| GET | `/kb/documents/{doc_id}/file` | ✅ | 下载/预览原始文件 |
| GET | `/kb/documents/{doc_id}/segments` | ✅ | 列出文档分段 |
| GET | `/kb/documents/{doc_id}/pages` | ✅ | 按页码平铺文档 |
| GET | `/kb/documents/{doc_id}/pages/{page_number}` | ✅ | 单页详情 |
| DELETE | `/kb/documents/{doc_id}` | ✅ | 删除文档 |

**上传文档**（multipart/form-data）：
- `file`：文件（支持 txt / md / csv / json / html / pdf / docx / png / jpg / gif / webp）
- `collection_id`：可选分区 ID

**上传响应**（201）：
```json
{
  "message": "上传成功，正在处理中",
  "batch_id": "batch_xxx",
  "document_id": "doc_xxx",
  "id": "doc_xxx",
  "file_name": "高等数学.pdf",
  "dataset_id": "ds_xxx",
  "collection_id": "col_xxx",
  "status": "indexing",
  "segment_status": "not_started",
  "parse_warning": null,
  "ocr_processed": false,
  "ocr_status": null,
  "ocr_current_page": null,
  "ocr_total_pages": null
}
```

**文档列表查询**：`GET /kb/documents?page=1&limit=20&collection_id=col_xxx`

**索引进度响应**：
```json
{
  "batch_id": "batch_xxx",
  "status": "completed",
  "error_message": null,
  "completed_segments": 45,
  "total_segments": 45
}
```

### 4.3 配置查询

| 方法 | 路径 | 状态 | 说明 |
|------|------|------|------|
| GET | `/kb/config` | ✅ | 查询 RAG 后端类型、上传限制、支持格式 |

---

## 5. 知识追踪 (KT)

**路由前缀**：`/api/v1/kt`
**鉴权**：是
**数据源**：TCN 引擎层（port 8001），`zhixu-v4.md` v2.0 治理
**重要变更**：v2.0 已完成 TCN 源码级确认对接，所有字段格式、类型、node_id 格式均已核实。

### 5.1 已淘汰接口（返回 404）

| 方法 | 路径 | 原功能 | TCN 替代方案 |
|------|------|--------|-------------|
| POST | `/kt/correct` | ❌ LADL 认知状态修正 | `POST /v1/user/predict`（由 Chat 内部异步调用） |
| POST | `/kt/evaluate` | ❌ LVR 评估 | predict 响应内嵌 `lvr` + `vs` + `diagnosis` |

### 5.2 学习路径推荐

| 方法 | 路径 | 状态 | 说明 |
|------|------|------|------|
| POST | `/kt/learning-path` | 🔄 已改造 | 数据源切换为 TCN `report` + 图谱缓存，推荐逻辑移至 `kt_service.py` |

**请求体**：
```json
{
  "states": {"math:addition": 0.95, "math:linear_equation": 0.42},
  "top_k": 5
}
```

> 注：`states` 字段保留兼容，但后端实际从 TCN `report` 自动获取掌握度数据。未来可废弃 `states`，仅保留 `top_k`。

**响应**：
```json
{
  "recommendations": [
    {
      "skill_id": "math:linear_equation",
      "skill_name": "一元一次方程",
      "current_mastery": 0.42,
      "importance": 3,
      "priority_score": 3.58
    },
    {
      "skill_id": "math:quadratic_equation",
      "skill_name": "一元二次方程",
      "current_mastery": 0.55,
      "importance": 2,
      "priority_score": 2.45
    }
  ]
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `skill_id` | string | 节点标识，格式 `"domain:node_id"`（如 `"math:linear_equation"`） |
| `skill_name` | string | 节点中文名称（来自图谱缓存 `/admin/graph/data/{domain}`） |
| `current_mastery` | float | 当前掌握度 0–1 |
| `importance` | int | 重要性（被多少后继节点依赖） |
| `priority_score` | float | 优先得分 = importance + (1 - mastery) |

---

### 5.3 先修关系查询

| 方法 | 路径 | 状态 | 说明 |
|------|------|------|------|
| POST | `/kt/prerequisites` | 🔄 已改造 | 先修/后继关系从图谱缓存获取，父节点掌握度来自 TCN `report` |

**请求体**：
```json
{
  "skill_id": "math:linear_equation"
}
```

**响应**：
```json
{
  "skill": {
    "id": "math:linear_equation",
    "name": "一元一次方程"
  },
  "prerequisites": [
    {
      "id": "math:addition",
      "name": "加法",
      "mastery": 0.95
    },
    {
      "id": "math:subtraction",
      "name": "减法",
      "mastery": 0.87
    }
  ],
  "dependents": [
    {
      "id": "math:quadratic_equation",
      "name": "一元二次方程",
      "mastery": 0.85
    },
    {
      "id": "math:function_basics",
      "name": "函数基础",
      "mastery": 0.72
    }
  ]
}
```

> **数据来源**：`parents` → TCN report 的 `parents` dict（`{parent_id: mastery}`）；`dependents` → 图谱缓存反向查询。

---

### 5.4 技能图谱

| 方法 | 路径 | 状态 | 说明 |
|------|------|------|------|
| GET | `/kt/skill-graph` | 🔄 已改造 | 图谱缓存全量节点+边，叠加 TCN `report` 用户掌握度 |

**响应**：
```json
{
  "skills": [
    {
      "id": "math:addition",
      "name": "加法",
      "mastery": 0.95,
      "confidence": 0.0
    },
    {
      "id": "math:linear_equation",
      "name": "一元一次方程",
      "mastery": 0.42,
      "confidence": 0.72
    },
    {
      "id": "math:calculus_intro",
      "name": "微积分入门"
    }
  ],
  "edges": [
    {"source": "math:addition", "target": "math:linear_equation"},
    {"source": "math:linear_equation", "target": "math:quadratic_equation"}
  ],
  "total_skills": 503,
  "total_edges": 1200
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `skills[].id` | string | 节点标识，格式 `"domain:node_id"` |
| `skills[].name` | string | 节点中文名称 |
| `skills[].mastery` | float? | 用户掌握度 0–1（仅练习过的节点有此字段） |
| `skills[].confidence` | float? | 可信度（rule 模式始终 0.0，LEKT/GKT/AKT 模式有值） |
| `edges` | array | 先修边，source → target 方向 |
| `total_skills` | int | 图谱总节点数 |
| `total_edges` | int | 图谱总边数 |

> ⚠️ `confidence` 在当前 rule 模式下始终为 0.0。`mastery` 仅出现在用户实际练习过的节点上，未练习节点不包含此字段（默认掌握度 0.5）。

---

### 5.5 认知状态查询（v2.0 新增）

| 方法 | 路径 | 状态 | 说明 |
|------|------|------|------|
| GET | `/kt/states` | ➕ 已实现 | 映射 TCN `report.nodes` → `{node_id: mastery}` |

**响应**：
```json
{
  "math:addition": 0.95,
  "math:linear_equation": 0.42,
  "math:quadratic_equation": 0.85,
  "math:factoring": 0.78
}
```

> 仅包含用户**实际练习过**的节点（TCN report 稀疏存储）。未练习过的节点默认掌握度 0.5，不出现在此响应中。

---

## 6. 题目管理 (Questions)

**路由前缀**：`/api/v1/questions`
**鉴权**：是

| 方法 | 路径 | 状态 | 说明 |
|------|------|------|------|
| POST | `/questions/generate` | ✅ | 对文档/分段批量 AI 出题 |
| POST | `/questions/generate-from-pages` | ✅ | 对选中页批量 AI 出题 |
| POST | `/questions/extract-from-pages` | ✅ | 从选中页提取教材自带题目 |
| GET | `/questions` | ✅ | 列出用户可见题目（可按 document_id / collection_id 过滤） |
| GET | `/questions/{question_id}` | ✅ | 单题详情（含溯源 provenance） |
| DELETE | `/questions?document_id=xxx` | ✅ | 删除指定文档的题库引用 |
| DELETE | `/questions/bulk` | ✅ | 批量删除题库引用 |

**生成题目请求**：
```json
{
  "document_id": "doc_xxx",
  "segment_ids": ["seg_001", "seg_002"]
}
```

---

## 7. 刷题 (Quiz)

**路由前缀**：`/api/v1/quiz`
**鉴权**：是

| 方法 | 路径 | 状态 | 说明 |
|------|------|------|------|
| POST | `/quiz/sessions` | ✅ | 创建刷题会话 |
| GET | `/quiz/sessions/{session_id}` | ✅ | 获取会话进度（不含标准答案） |
| POST | `/quiz/sessions/{session_id}/answers` | ✅ | 提交单题答案 |
| GET | `/quiz/sessions/{session_id}/results` | ✅ | 汇总错题与 unknown 题目 |

**创建会话请求**：
```json
{
  "document_id": "doc_xxx",
  "collection_id": "col_xxx",
  "question_ids": ["q_001", "q_002"],
  "question_count": 10
}
```

**提交答案请求**：
```json
{
  "question_id": "q_001",
  "user_answer": "B. 42",
  "status": "answered",
  "time_spent_seconds": 45
}
```

> `status`：`"answered"` / `"unknown"`（表示"我不会"）

**答题结果响应**：
```json
{
  "answer_id": "ans_xxx",
  "question_id": "q_001",
  "status": "correct",
  "correct_answer": "B. 42",
  "explanation": "因为 6 × 7 = 42...",
  "citation": {
    "document_id": "doc_xxx",
    "document_name": "高数.pdf",
    "segment_index": 5,
    "content_snippet": "乘法运算定律..."
  }
}
```

**会话结果响应**：
```json
{
  "session_id": "sess_xxx",
  "total_questions": 10,
  "correct_count": 7,
  "wrong_count": 2,
  "unknown_count": 1,
  "answers": [
    {
      "question_id": "q_001",
      "status": "correct",
      "correct_answer": "B. 42",
      "explanation": "...",
      "citation": {...}
    }
  ]
}
```

---

## 8. 辅导 (Tutor)

**路由前缀**：`/api/v1/tutor`
**鉴权**：是

| 方法 | 路径 | 状态 | 说明 |
|------|------|------|------|
| POST | `/tutor/sessions` | ✅ | 从题目创建苏格拉底式辅导会话 |
| GET | `/tutor/sessions/{session_id}` | ✅ | 获取辅导会话详情与对话历史 |
| POST | `/tutor/sessions/{session_id}/messages` | ✅ | 发送消息（支持 SSE 流式） |

**创建会话请求**：
```json
{
  "question_id": "q_001",
  "document_id": "doc_xxx"
}
```

**发送消息请求**：
```json
{
  "content": "我不太理解为什么答案是 42",
  "stream": true
}
```

---

## 9. 首页建议 (Dashboard)

**路由前缀**：`/api/v1/dashboard`
**鉴权**：是

| 方法 | 路径 | 状态 | 说明 |
|------|------|------|------|
| GET | `/dashboard/suggestions` | ✅ | 基于知识库文档 LLM 生成 2-3 条个性化建议 |

**响应**：
```json
{
  "suggestions": [
    "你的高等数学文档已上传 3 周，建议回顾极限概念",
    "微积分章节错题率较高，可以试试针对性训练"
  ]
}
```

---

## 10. 学习分析 (Analytics)

**路由前缀**：`/api/v1/analytics`
**鉴权**：是

| 方法 | 路径 | 状态 | 说明 |
|------|------|------|------|
| GET | `/analytics/stats` | ✅ | 汇总文档、题库与刷题进度 |
| GET | `/analytics/tag-stats` | ✅ | 按 tag 与题型聚合错题统计 |
| POST | `/analytics/learning-report` | ✅ | 生成 LLM 学习报告并保存到笔记（别名路由） |

**stats 响应**：
```json
{
  "total_documents": 15,
  "total_questions": 120,
  "quiz_sessions_count": 8,
  "overall_accuracy_rate": 72,
  "documents_by_zone": {
    "study": 12,
    "life": 3
  },
  "questions_by_type": {
    "choice": 60,
    "fill": 40,
    "essay": 20
  }
}
```

---

## 11. 学习报告 (Reports)

**路由前缀**：`/api/v1/reports`
**鉴权**：是

| 方法 | 路径 | 状态 | 说明 |
|------|------|------|------|
| POST | `/reports/generate` | ✅ | 基于 tag 统计与刷题数据生成 LLM 学习报告 |
| GET | `/reports` | ✅ | 列出历史学习报告 |
| GET | `/reports/latest` | ✅ | 获取最新一份学习报告 |
| GET | `/reports/{report_id}` | ✅ | 获取指定学习报告 |

---

## 12. 针对训练 (Training)

**路由前缀**：`/api/v1/training`
**鉴权**：是

| 方法 | 路径 | 状态 | 说明 |
|------|------|------|------|
| POST | `/training/targeted/start` | ✅ | Agent 制定训练计划：选题 + 创建刷题会话 |
| GET | `/training/targeted/reports/{report_id}/active-session` | ✅ | 查询某报告下未完成的训练会话 |
| GET | `/training/targeted/sessions/{session_id}` | ✅ | 恢复训练会话 |
| POST | `/training/targeted/tutor/{agent_session_id}` | ✅ | 训练页 AI 辅导（支持 SSE 流式） |

**开始训练请求**：
```json
{
  "report_id": "rpt_xxx",
  "force_new": false
}
```

---

## 13. 个人画像 (Profile)

**路由前缀**：`/api/v1/profile`
**鉴权**：是
**状态**：📋 待实现（迭代 1，见 `zhixu-v2.md`）
**数据来源**：软件层自有 PostgreSQL（与 TCN UserMask 互补，存储非算法特征）

| 方法 | 路径 | 状态 | 说明 |
|------|------|------|------|
| GET | `/profile` | 📋 | 查询当前个人画像 |
| POST | `/profile/build` | 📋 | 创建画像构建任务 |
| GET | `/profile/tasks/{task_id}` | 📋 | 查询构建任务状态与确认问题 |
| POST | `/profile/tasks/{task_id}/confirm` | 📋 | 提交确认回答 |
| GET | `/profile/graph` | 📋 | 获取画像图谱（节点+可信度+依据来源） |
| POST | `/profile/nodes/{node_id}/feedback` | 📋 | 提交画像节点不准确反馈 |

**Profile 数据结构**：
```json
{
  "uid": 1,
  "learning_style": "example-driven",
  "communication_style": "casual",
  "preferred_explanation": "analogy",
  "known_interests": ["数学", "物理"],
  "onboarding_completed": true,
  "profile_confidence": 0.72,
  "last_updated": "2026-07-13T10:00:00Z",
  "dialogue_history_summary": "用户倾向于用生活场景理解抽象概念，不喜欢符号堆砌"
}
```

---

## 14. 笔记系统 (Notes)

**路由前缀**：`/api/v1/notes`
**鉴权**：是
**状态**：📋 待实现（迭代 2，`UserNote` 模型已存在）

| 方法 | 路径 | 状态 | 说明 |
|------|------|------|------|
| POST | `/notes` | 📋 | 创建笔记 |
| GET | `/notes` | 📋 | 列出笔记（分页，可按 collection_id 过滤） |
| GET | `/notes/{note_id}` | 📋 | 获取笔记详情 |
| PATCH | `/notes/{note_id}` | 📋 | 更新笔记 |
| DELETE | `/notes/{note_id}` | 📋 | 删除笔记 |
| GET | `/notes/search?q=xxx` | 📋 | 全文搜索笔记 |
| POST | `/notes/from-chat` | 📋 | 从 AI 回答/资料片段生成笔记 |

**笔记数据结构**：
```json
{
  "id": "note_xxx",
  "user_id": 1,
  "collection_id": "col_xxx",
  "title": "微积分重点回顾",
  "content_md": "# 微积分\n\n## 极限...",
  "note_type": "manual",
  "created_at": "2026-07-15T10:00:00Z",
  "updated_at": "2026-07-15T12:00:00Z"
}
```

> `note_type`：`"manual"` / `"ai_generated"` / `"from_chat"`

---

## 15. 提醒系统 (Reminders)

**路由前缀**：`/api/v1/reminders`
**鉴权**：是
**状态**：📋 待实现（迭代 2）

| 方法 | 路径 | 状态 | 说明 |
|------|------|------|------|
| POST | `/reminders` | 📋 | 创建提醒 |
| GET | `/reminders` | 📋 | 列出提醒（分页） |
| PATCH | `/reminders/{reminder_id}` | 📋 | 更新提醒状态（完成/延后/关闭） |
| DELETE | `/reminders/{reminder_id}` | 📋 | 删除提醒 |

**提醒数据结构**：
```json
{
  "id": "rem_xxx",
  "user_id": 1,
  "title": "复习微积分",
  "description": "重点复习导数章节",
  "rrule": "FREQ=WEEKLY;BYDAY=MO",
  "next_trigger": "2026-07-20T09:00:00Z",
  "status": "active",
  "linked_type": "document",
  "linked_id": "doc_xxx",
  "created_at": "2026-07-15T10:00:00Z",
  "updated_at": "2026-07-15T10:00:00Z"
}
```

> `linked_type` 枚举：`"document"` / `"knowledge_node"` / `"learning_path"` / `"note"`

---

## 16. 通知中心 (Notifications)

**路由前缀**：`/api/v1/notifications`
**鉴权**：是
**状态**：📋 待实现（迭代 2）

| 方法 | 路径 | 状态 | 说明 |
|------|------|------|------|
| GET | `/notifications` | 📋 | 列出通知（分页+分类+未读筛选） |
| PATCH | `/notifications/{id}/read` | 📋 | 标记单条已读 |
| PATCH | `/notifications/read-all` | 📋 | 标记全部已读 |
| GET | `/notifications/unread-count` | 📋 | 获取未读数量 |

**通知类型**：`kb_indexed`（KB 索引完成）、`transcription_complete`（转写完成）、`profile_built`（画像构建完成）、`reminder_due`（提醒到期）、`system`（系统消息）

---

## 17. 统一搜索 (Search)

**路由前缀**：`/api/v1/search`
**鉴权**：是
**状态**：📋 待实现（迭代 2）

| 方法 | 路径 | 状态 | 说明 |
|------|------|------|------|
| GET | `/search?q=xxx&type=kb_doc,kb_segment,note,tag,knowledge_node` | 📋 | 跨域统一搜索 |

**响应**：
```json
{
  "results": [
    {
      "type": "kb_doc",
      "id": "doc_xxx",
      "title": "高等数学.pdf",
      "snippet": "...微积分是研究<em>函数</em>变化率的数学分支...",
      "link": "/kb/documents/doc_xxx",
      "created_at": "2026-07-15T00:00:00Z"
    }
  ],
  "total": 12,
  "page": 1
}
```

---

## 18. 学习路径 (Learning Path)

**路由前缀**：`/api/v1/learning-path`
**鉴权**：是
**状态**：📋 待实现（迭代 2，依赖 TCN `gaps` 接口）

| 方法 | 路径 | 状态 | 说明 |
|------|------|------|------|
| POST | `/learning-path` | 📋 | 基于 TCN `gaps` + LLM 创建学习路径 |
| GET | `/learning-path` | 📋 | 列出用户所有学习路径 |
| PATCH | `/learning-path/{id}/steps/{step_id}` | 📋 | 标记步骤状态（start/complete/skip/postpone） |
| POST | `/learning-path/{id}/reorder` | 📋 | 手动或自动重排路径 |
| GET | `/learning-path/history` | 📋 | 已完成的学习路径历史 |

---

## 19. 数据同步 (Sync)

**路由前缀**：`/api/v1/sync`
**鉴权**：是
**状态**：📋 待实现（迭代 1）

| 方法 | 路径 | 状态 | 说明 |
|------|------|------|------|
| POST | `/sync/export` | 📋 | 导出加密数据包到对象存储，返回 download_url |
| POST | `/sync/restore` | 📋 | 从云端恢复 Profile + KB 索引 + 文件引用 |
| DELETE | `/sync/cloud-backup` | 📋 | 下载确认后清除云端副本 |

---

## 20. 系统健康 (Health)

| 方法 | 路径 | 鉴权 | 状态 | 说明 |
|------|------|------|------|------|
| GET | `/health` | 否 | ✅ | 应用健康检查 |

**响应**：
```json
{
  "status": "ok",
  "skills_count": 503,
  "model_loaded": true
}
```

> `model_loaded` 反映我方后端整体状态（含 TCN 引擎连通性），而非本地 LEKT 加载状态。

---

## 附录 A：状态码规范

| 状态码 | 含义 | 使用场景 |
|--------|------|---------|
| 200 | 成功 | GET / PATCH 请求成功 |
| 201 | 已创建 | POST 创建资源成功 |
| 400 | 请求错误 | 参数校验失败、业务规则不满足 |
| 401 | 未授权 | Token 缺失 / 无效 / 过期 |
| 404 | 未找到 | 资源不存在或接口已淘汰 |
| 409 | 冲突 | 重复创建资源 |
| 413 | 请求体过大 | 文件超过大小限制 |
| 422 | 不可处理的实体 | Pydantic 校验失败 |
| 429 | 请求过多 | API 配额耗尽 |
| 500 | 服务器错误 | 内部异常 |
| 503 | 服务不可用 | TCN / LLM 等依赖不可达 |

---

## 附录 B：TCN 外部接口参考

> TCN 引擎层（port 8001）为独立服务，我方后端通过 HTTP 调用。以下为已核实接口（源码级确认，`TCN_API_CONFIRMATION_REPLY.md` v1.4）。
> **OpenAPI 文档**：`http://127.0.0.1:8001/docs`

### B.1 已提供接口

| 方法 | 端点 | 鉴权 | 我方调用位置 | 说明 |
|------|------|------|------------|------|
| POST | `/v1/user/predict` | 否 | Chat/Tutor 答题后异步调用 | 更新知识状态，返回 lvr/vs/diagnosis/node_mastery(前10节点)/recommended_backtrack |
| GET | `/v1/user/profile/{user_hash}` | 否 | 认知画像摘要查询 | 返回 total_steps/global_lvr/graph_version/node_count |
| GET | `/v1/user/report/{user_hash}` | 否 | KT 所有改造接口数据源 | 返回顶层 object：user_hash/global_lvr/total_steps/nodes(dict) |
| GET | `/health` | 否 | 启动时健康探测 | 返回 status/nodes/graph_version/constraint/model |
| GET | `/admin/graph/domains` | X-Admin-Token | 启动时拉取图谱 | 所有域名列表 |
| GET | `/admin/graph/data/{domain}` | X-Admin-Token | 启动时拉取图谱 | 单域全量节点(name+id+status)+边(from/to/weight/relation) |

### B.2 待新增接口（2 天内交付，草案）

| 方法 | 端点 | 优先级 | 用途 |
|------|------|--------|------|
| GET | `/v1/user/summary/{user_hash}` | P0 | LLM System Prompt 注入（mastered_count/total_count/weakest_domain/most_active_node） |
| GET | `/v1/user/gaps/{user_hash}` | P1 | 先修断层查询（按 severity 降序的节点列表） |
| GET | `/v1/user/vulnerabilities/{user_hash}` | P1 | 认知脆弱点（伪掌握）预警 |
| GET | `/v1/user/lvr_alert/{user_hash}` | P1 | LVR 预警状态（alert_level + top5 violations） |

### B.3 predict 请求/响应格式（完整）

**请求**（7 字段）：
```json
{
  "api_key": "",
  "user_hash": "a3f9c1d2e8b7...",
  "domain_id": "math",
  "current_node": "math:quadratic_equation",
  "user_action": "correct",
  "step_index": 12,
  "session_id": "sess_20260713_001"
}
```

**响应**（8 字段）：
```json
{
  "user_hash": "a3f9c1d2e8b7...",
  "lvr": 0.12,
  "vs": 0.03,
  "diagnosis": "LVR=0.120: quadratic(0.85) > linear(0.40) [3 edges violated]",
  "recommended_backtrack": "math:linear_equation",
  "node_mastery": {"math:arithmetic": 0.95, "...": "..."},
  "epsilon_used": 0.05,
  "training_phase": "unknown"
}
```

> ⚠️ `node_mastery` 是图谱前 10 个节点（索引 0–9），不保证包含 `current_node`。需当前节点最新掌握度请调 `report`。

### B.4 report 响应格式（完整）

```json
{
  "user_hash": "a3f9c1d2e8b7...",
  "global_lvr": 0.12,
  "total_steps": 47,
  "nodes": {
    "math:quadratic_equation": {
      "mastery": 0.85,
      "confidence": 0.72,
      "parents": {
        "math:linear_equation": 0.91,
        "math:factoring": 0.78
      }
    }
  }
}
```

> ⚠️ 顶层是 **object**（不是 array）。`nodes` 是 dict（key=`"domain:node_id"`），仅含用户练习过的节点（稀疏）。`parents` 是 dict（`{parent_id: mastery_float}`）。

---

## 更新记录

| 日期 | 版本 | 变更 |
|------|------|------|
| 2026-07-15 | v3.1 | 基于 `zhixu-v4.md` v2.0（TCN 源码确认）修订：修正 KT 所有响应 node_id 格式为 `domain:node_id`；更新 skill-graph 新增 mastery/confidence 字段说明；补充 TCN 附录 B（完整 predict/report 格式）；标注 C1-C5 纠正项 |
| 2026-07-15 | v3.0 | 初始版本：淘汰 `/kt/correct` `/kt/evaluate`；改造 3 个 KT 接口；新增 `/kt/states`；标注 7 个待实现模块 |

---

## 附录 C：Web 前端接入审计（2026-07-15）

本附录记录 `D:\桌面\zhishi-web` 对本版后端清单的真实接入状态。状态含义：

- `已接入`：已有 `src/lib/api` 封装、真实页面消费者和对应用户流程测试。只有端点契约测试不能单独视为页面接入完成。
- `后端待实现`：本清单明确标记为待实现，前端不得把本地 demo 描述成云端能力。
- `已移除旧调用`：旧接口已淘汰，前端运行时代码不再请求该路径。

| 接口域 | 后端状态 | Web 前端状态 | 主要实现 |
| --- | --- | --- | --- |
| Auth | 已实现 | 已接入 | `src/lib/api/auth.ts`、`src/lib/auth/AuthContext.tsx` |
| Plan | 已实现 | 已接入 | `src/lib/api/plan.ts`、设置页 |
| Chat | 已实现 | 已接入 | `src/lib/api/chat.ts`、AI 对话页；请求范围映射为 `collection_id`，引用字段统一适配 |
| KB Collections / Categories | 已实现 | 已接入 | `src/lib/api/kb.ts`、分区选择与管理弹窗；支持创建、重命名、删除 |
| KB Documents / Pages / Segments / File | 已实现 | 已接入 | 知识库分页、预览、单页查看、原文件下载、删除与上传轮询 |
| KT | 已改造 | 已接入 | `src/lib/api/kt.ts`、图谱、画像、学习分析、学习路径 |
| Questions | 已实现 | 已接入 | 知识库出题、按页生成/提取、游标加载、批量删除与按文档清空 |
| Quiz | 已实现 | 已接入 | 创建/恢复会话、提交答案、后端结果汇总、普通题目下一批 |
| Tutor | 已实现 | 已接入 | 创建/恢复会话、历史消息恢复、连续 SSE 辅导 |
| Dashboard | 已实现 | 已接入 | `src/lib/api/dashboard.ts`、首页 |
| Analytics | 已实现 | 已接入 | `src/lib/api/analytics.ts`、学习分析页 |
| Reports | 已实现 | 已接入 | 学习分析页支持生成、历史列表、最新/指定报告详情 |
| Training | 已实现 | 已接入 | 从报告创建/恢复训练，刷题页恢复其 Quiz，并使用训练专属 Tutor SSE |
| Health | 已实现 | 已接入 | `src/lib/api/health.ts`、登录页/设置页/诊断页 |
| Profile | 待实现 | 后端阻塞 | 当前画像页只消费 KT 认知图谱；不能声明软件层个人画像 CRUD 已完成 |
| Notes | 待实现 | 后端阻塞，开发模式有本地闭环 | `src/lib/api/notes.ts` 保留目标契约；真实跨设备 CRUD 尚不可验证 |
| Reminders | 待实现 | 后端阻塞，当前为本地 demo | 提醒页不应描述为服务端同步能力 |
| Notifications | 待实现 | 后端阻塞，端点封装已按 `PATCH` 契约校准 | `src/lib/api/notifications.ts`；UI 当前仍使用本地通知 |
| Search | 待实现 | 后端阻塞 | 顶栏搜索尚无统一搜索后端可接 |
| Learning Path `/api/v1/learning-path` | 待实现 | 后端阻塞 | 当前 `/path` 使用已实现的 `/api/v1/kt/learning-path` 推荐接口 |
| Sync | 待实现 | 后端阻塞 | 当前 Web 不具备云端一次性迁移闭环 |

已淘汰接口处理：

- `/api/v1/kt/correct`、`/api/v1/kt/evaluate` 已从前端运行时代码移除。
- 旧 `/api/v1/questions/{question_id}/answer`、`/api/v1/questions/{question_id}/explain` 已迁移到 Quiz + Tutor。
- 前端不再向 Chat 发送本版未声明的 `mode`、`category_id`、`note_id`、`context` 字段；知识库范围统一发送 `collection_id`。
- `training_session_id` 由学习分析页传给刷题页；刷题页读取 `quiz_session_id` / `agent_session_id` 恢复后端选题和训练辅导上下文。
- Questions 列表使用 `next_cursor` 分批消费；KB 文档列表使用后端 `page` / `limit` 分页，不再固定只展示首批数据。
