# 罗洁这一期

完整口径看 [next.md](./next.md)、[功能更新.md](./功能更新.md)。合同和验收勾选在 GitHub Issue 里，不要只凭这份摘要开工。

- 缩写 / 分支：`RJ` → `feature/rj-<英文短名>`
- **从 `origin/develop` 拉出自己的分支再改**，不要从 `feature/wangchuri-RAG-keyword-search` 拉。PR 打向 `develop`。
- 新表用 Alembic，不要只靠 `create_all`

```bash
git fetch origin
git checkout develop
git pull origin develop
git checkout -b feature/rj-<英文短名>
```

## 你做的

**第一期：功能 A — 目标能被记住**

Issue：[#15 Goal](https://github.com/pfx43/zhixu-backend/issues/15)

- 表 `goals`（那句话、属性、有效期、`active/paused/completed`）。一人同时只有一条 active。
- `GET/PUT /api/v1/me/profile`（称呼）
- `GET /api/v1/goals`、`PUT /api/v1/goals/active`
- 引导结束写入 nickname + 一条 active 目标；老五步可以跳过，不要再挡学习
- 删号时删掉这个用户的 goals

目标太短要不要追问，按设计稿 `onboarding.html`，有疑问再问派活的人。

**和第二期交接：** 陈勇搏做 `daily_tasks` 时，删号级联你帮看表结构（Issue [#19](https://github.com/pfx43/zhixu-backend/issues/19)）。你不写检查器、不写 Tina 工具。

**TCN（第二队列）：** Issue [#53](https://github.com/pfx43/zhixu-backend/issues/53)。对面 `register` 好了之后：知序注册成功调建档；删号若有注销就调。环境变量 `TCN_API_KEY`。挂了要降级，不能打挂整站。规则：[TCN集成.md](./TCN集成.md)。不改出题 Agent。先把 [#43](https://github.com/pfx43/zhixu-backend/pull/43) rebase 绿并合入（#28 #36 #37 #39）。

**文档：** 后端 OpenAPI / 接口说明随 PR 写清。产品向的功能更新正文由彭飞翔整理，见 [彭飞翔.md](./彭飞翔.md)。

## 别做

- 不要改 Tina 工具
- 不要做向量化
- 不要做今日任务表、勾选完成、日历、成就
- 不要用 `dashboard/suggestions` 冒充目标
- 不要改前端仓库（前端是彭飞翔）

## 做完别人才能做

- 陈勇搏 [#19 Task](https://github.com/pfx43/zhixu-backend/issues/19) 要把任务挂在 goal 上
- 陈勇搏 [#20 Tina](https://github.com/pfx43/zhixu-backend/issues/20) 要读 / 改这一条目标

## 共同规矩

查询必须带当前登录用户。完成弹窗文案不是你这块，不要改。页码不许猜、不能勾选、不向量化——验收时按这三条拍。
