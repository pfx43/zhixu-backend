# 张子麟这一期

完整口径看 [next.md](./next.md)、[功能更新.md](./功能更新.md)。合同和验收勾选在 GitHub Issue 里，不要只凭这份摘要开工。

- 缩写 / 分支：`ZL` → `feature/zl-<英文短名>`
- **从 `origin/develop` 拉出自己的分支再改**，不要从 `feature/wangchuri-RAG-keyword-search` 拉。PR 打向 `develop`。
- 新表 / 新列用 Alembic，不要只靠 `create_all`

```bash
git fetch origin
git checkout develop
git pull origin develop
git checkout -b feature/zl-<英文短名>
```

## 你做的

**第一期后半：功能 B — 段落带页码 + 章节目录**（可和罗洁的 A 并行）

Issue：[#16 KB](https://github.com/pfx43/zhixu-backend/issues/16)

- 切段时写下 `page_start` / `page_end`（一段可以跨页）
- 关键词检索命中带上书 id 和这些页码；没有页码不许猜
- 抽出目录：**章 → 页**，给出土题页按章分组勾选。优先用书里已有的目录 / 标题 / 书签。不要只把前几页丢给 Agent
- 旧书补一次段页码，不能只对新上传生效

**TCN（第二队列）：** Issue [#51](https://github.com/pfx43/zhixu-backend/issues/51)。目录提取同一趟只判学科，写成书上的 `tcn_domain`：封闭名单（`higher_math` / `math` / `physics` / `discrete_math`）或 `None`。拿不准就 `None`，不要猜、不要自造学科名。页码规则不变。不要写出题 prompt、不要调 `predict`。规则：[TCN集成.md](./TCN集成.md)。先收口 [#44](https://github.com/pfx43/zhixu-backend/issues/44) [#45](https://github.com/pfx43/zhixu-backend/issues/45)。

接到出题只有一条路：程序拿 `document_id` + 页码，调已经有的按页出题。不要把搜到的那段话丢给另一个 Agent 再猜页。

**不要抄知拾的 learning_path 当目录**：那份只有章标题、没有页范围。

**第四期：** 进度 / 学习路径需要的**后端数据**你出。页面由彭飞翔接，见 [彭飞翔.md](./彭飞翔.md)。不要改前端仓库。

## 跟陈勇搏怎么切开

题目入库的 `page_number`、出题页 SSE 进度、`max_pages_per_gen=10`、邻页工具，是他的 [#17 Question](https://github.com/pfx43/zhixu-backend/issues/17)。你出章→页和段上页码；他对齐出题写入和出题页。出题接口不要推翻。

## 别做

- 不要做向量化、向量数据库、外置向量服务、Chroma
- 不要上传完自动整本出题
- 不要改 Tina 对话工具（那是陈勇搏 D）
- 不要把 `submit_question` 挂到主对话
- 不要改前端仓库（前端是彭飞翔）

## 做完别人才能做

- [#19 Task](https://github.com/pfx43/zhixu-backend/issues/19) 按页判定完成
- [#20 Tina](https://github.com/pfx43/zhixu-backend/issues/20) 看目录 / 按检索出题（页码必须来自你入库的字段）

## 共同规矩

查询必须带当前登录用户。页码不许猜、不能勾选、不向量化。
