# 知序后端下一期（给开发看）

给学弟学妹认领用。先看 [index.md](./index.md) 找自己的文档。产品方向仍是王晨那份 [闭环与更新方案.md](./闭环与更新方案.md)。

闭环就三句话：目标决定 Tina 能派什么；书和答题记录决定派哪一件；用户去做上传/出题/刷题，程序看见结果就宣布完成。用户不能自己打勾。弹窗固定：「该任务已经完成！」

---

## 0. 向量化：不做

向量化 = 把书的每一段算成一串数字，用算力找「意思相近」的段落。服务器扛不住这种计算，**这一期不要做，也不要为了闭环再加回来。**

聊天检索已经有替代：`RAG_BACKEND=keyword`，在数据库里对段落做关键词匹配。够用。

检索要能接到出题，缺的不是向量，是**每一段要带页码**（见功能 B）。没有页码，Tina 搜到一段也不知道该出第几页，只能瞎猜——不许猜。

张子麟以前分工里的「向量外置」这一期取消，改做功能 B。

---

## 1. 已经有的基础能力（当积木，不要重写）

后面每个新功能都写「站在哪些积木上」。积木本身只修衔接，不推翻。


| 编号  | 积木      | 现在能干什么                                | 主要代码                                       |
| --- | ------- | ------------------------------------- | ------------------------------------------ |
| 登   | 登录 / 用户 | token、昵称、删号                           | `auth`、`users`                             |
| 引   | 引导      | 老五步问卷，没有「那句目标」                        | `onboarding`                               |
| 书   | 上传存书    | 存文件、按 hash 去重、抽出文字、切段                 | `kb_service`、`segment_service`             |
| 页   | 按页列表    | `GET .../documents/{id}/pages`，给人勾选   | `page_service`                             |
| 题   | 按页出题    | `POST /questions/generate-from-pages` | `question_gen_service`                     |
| 刷   | 刷题      | 开会话、对/错/不会                            | `quiz_service`                             |
| 辅   | 辅导      | 刷题时问 Tina                             | `tutor`                                    |
| 聊   | 聊天检索    | Tina 在你的书里搜段落（关键词）                    | `ZhixuAgent` + `keyword_retrieval_service` |
| 量   | 计量      | token / 次数，不是学习任务                     | `usage_*`                                  |


首页那条 `dashboard/suggestions`（LLM 写三条建议）**不是积木**，不要改名当今日任务。  
`training_plans` 是弱项组卷，也不是今日任务。

隔离已经有规矩：接口都走登录用户；查书、查题都带 `user_id`。新表、新工具照抄。不要学知拾那种默认用户 1。

---



## 2. 新功能怎么拆（一个功能一块，认领一块）

认领原则：先看「站在哪些积木上」，缺的积木没做完不要硬接。  
分支从 **`origin/develop`** 拉出，不要从 `feature/wangchuri-RAG-keyword-search` 或其他功能分支拉：`git fetch origin && git checkout develop && git pull && git checkout -b feature/<缩写>-<英文短名>`（RJ 罗洁 / CYB 陈勇搏 / ZL 张子麟）。PR 打向 `develop`。新表用 Alembic 迁移，不要只靠 `create_all`。

---



### 功能 A — 目标能被记住（第一期，先做）

**这是什么：** 引导或 Tina 确认一句长期目标，写入这个账号。刷新还在。首页只展示这一条进行中的目标。

**不是什么：** 不是每日待办，不是任务列表，不是「我学会了」按钮。

**站在哪些积木上：** 登、引（有用户、有引导状态，缺 goal 字段/表）。

**要新建：**

- 表 `goals`（`user_id`、那句话、`active/paused/completed`）。一人同时只有一条 active。
- `GET/PUT /api/v1/me/profile`（称呼）
- `GET /api/v1/goals`、`PUT /api/v1/goals/active`
- 引导结束：写下 nickname + 一条 active 目标（老五步可以跳过，不要再挡着学习）
- 删号时删掉这个用户的 goals

**谁做：** 罗洁（用户 / 引导 / 删号本来就是这块）。目标太短要不要追问，按设计稿 `onboarding.html`，有疑问再问派活的人。

**做完才轮到：** 功能 C（任务要挂在 goal 上）、功能 D（Tina 读/改目标）。

---



### 功能 B — 段落带页码 + 章节目录（第一期后半，和 A 可并行）

**这是什么：** 书切段进库时，每一段写上自己在第几页到第几页。另外抽出「第几章 → 哪些页」。Tina 检索或按章出题时，用这些页去调按页出题。

**不是什么：** 不是向量化。不是上传完自动整本出题。

**站在哪些积木上：** 书、页、题、聊。  
现在切段只记了「这段从全文第几个字到第几个字」，检索命中没有页码，出题接口却只要页码——对不上。

**要改：**

- 切段时：先有每页的文字范围，再给每段填 `page_start` / `page_end`（一段可以跨页）
- 关键词检索命中带上这些页码
- 旧书补一次段页码，不能只对新上传生效
- 解析后存章节目录（章 → 页），不要让 Tina 现场编页码。知拾本地的 learning_path 只有章标题、没有页范围，**不能当目录抄过来**
- 题目入库要写下页码（provenance / 等价字段），页列表才能显示「已出题 N」，检查器才能按页判定
- 一次出题用 `max_pages_per_gen` 截断（设计稿勾选上限 10，前后端同一数字，不要前端 10、后端 30）
- 页码必须来自目录或命中，禁止模型手填

接到出题只有一条路：程序拿 `document_id` + 页码，调用已经有的按页出题。不要把搜到的那段话丢给另一个 Agent 再猜页。不要只把前几页丢给 Agent 出一份没有页码的「学习路径」冒充目录。

**谁做：** 张子麟（切段页码、章节目录、旧书补页；原来「向量」换成这件事）。题目带 `page_number`、出题页进度跟陈勇搏出题这条对齐。出题接口本身不要推翻，加页数上限和按页写入。

**做完才轮到：** 功能 D 里 Tina 的「看目录 / 按检索出题」。功能 C 的「出题任务按页完成」也依赖页码。

---



### 功能 C — 今日任务能被判定（第二期）

**这是什么：** 当天 1～3 件事。Tina 派（第三期才自动派；第二期表和判定先通）。用户去做学习动作，程序判定完成。

**不是什么：** 不是建议卡片，不是针对训练，不是日历，不是让用户勾选。

**站在哪些积木上：** 书（上传）、题（按页出题）、刷（交卷）、功能 A（goal）、功能 B（按页算完成）。

**要新建：**

- 表 `daily_tasks`（属于哪个用户、哪条目标、哪一天、标题、理由、类型、payload、怎样算完成、状态）
- `GET /api/v1/tasks/today`、`POST /api/v1/tasks/today/ensure`（当天没有则按缺口生成；有未完成的先展示，不重复派）
- 检查器挂在已有成功路径后面，不要新开一套「完成 API」：
  - 上传成功 → 若今日任务是上传，且学习区多了一本**能解析的**书（对不上任务标题也算完成；缺口留给下一次派）
  - 按页出题：不要只看 HTTP 200（现在异步会立刻返回 `processing`）。等那些页上已经有题（题目必须带 `page_number`）。不要等整本书 `question_gen_status=completed`
  - 交卷成功 → 任务范围内交够数量（章刷 / 错题 / 不会）
- 这些接口的成功响应带 `completed_tasks: [{id, title}]`，空就不弹窗
- 删号时级联删 daily_tasks
- **不要**做「勾选完成」接口

**谁做：** 陈勇搏（以前把计量挂在调用后面，这一期把检查器挂在上传/出题/交卷后面，路子类似）。罗洁看删号和表结构。

**做完才轮到：** 功能 D（Tina 布置任务）、功能 E（首页读今日任务）。

---



### 功能 D — Tina 能派任务（第三期）

**这是什么：** 主对话里 Tina 会看目标和缺口，布置今天 1～3 件；也会按目录或检索命中去出题。

**不是什么：** 不是再做一个出题 Agent。聊天检索还在，用来讲题、引用原文。

**站在哪些积木上：** 聊、题、功能 A/B/C。没有 A 它不知道目标；没有 B 它不敢出题；没有 C 它无处可写任务。

**要加的工具（用户 id 只来自登录，不要让模型传入「查谁」）：**


| 工具                                | 干什么                   | 用哪些积木 |
| --------------------------------- | --------------------- | ----- |
| `get_active_goal` / `revise_goal` | 读/改那一条目标              | A     |
| `get_document_toc`                | 章 → 页，标出已有题的页         | B、题   |
| `generate_questions`              | 书 id + 页码 → 现有按页出题    | B、题   |
| `get_learning_gaps`               | 没书 / 有书没题 / 哪章未做、错、不会 | 书、题、刷 |
| `ensure_today_tasks`              | 按缺口写入当天任务             | C     |


任务叫什么、为什么派，Tina 写人话。  
这件事算哪一类、怎样算做完，**程序定**，不让模型发明规则。

派的顺序：没书 → 上传；有书没题 → 选一章的页出题；有题 → 按章刷或错/不会。好几本都能走才问碰哪本。

**谁做：** 陈勇搏（C 做完接着做；以前接过 Tina 包装层）。工具只调 A/B/C 已有接口，不要把出题内部的 `submit_question` 挂到主对话上。页码规则跟功能 B 对齐，对不上找张子麟。拍板/验收不写进认领。

---



### 功能 E — 设计稿接真接口（第四期，最后）

**这是什么：** `design/html/` 布局不变，数据改走 WSL 真 API。最后才迁壳（侧栏只留：首页、Tina、笔记、资料、进度、学习路径、设置占位）。不要扫描件解析。

**站在哪些积木上：** A/C/D 都通了再接。首页读目标和今日任务，不要接 suggestions。

**谁做：** 彭飞翔改前端，并整理 [功能更新.md](./功能更新.md)。陈勇搏 / 张子麟保证后端 API（任务、出题进度、章→页、进度数据）。后端同学不要改前端仓库。

---



### 功能 F — TCN 词表出题 + 交卷追踪（第二队列）

**这是什么：** 书在目录提取时标学科（或 `None`）；属于图谱学科的书，出题 tag 必须来自 TCN 词表；交卷把对/错传给引擎。规则和认领：[TCN集成.md](./TCN集成.md)。

**不是什么：** 不是 TCN 的 Learning OS。不是每个学生一把 API Key。不是聊天里传 `tc_node_id`。

**站在哪些积木上：** 书、题、刷、功能 B（目录提取同一趟写 `tcn_domain`）。

**要做：** 词表加载；书上 `tcn_domain`；出题校验 tag；交卷 `predict`（「不会」不传）；等对面 register 后注册建档。开关默认关。生产 bug 合入前不要插队。

**谁做：** 张子麟 [#51](https://github.com/pfx43/zhixu-backend/issues/51)；陈勇搏 [#52](https://github.com/pfx43/zhixu-backend/issues/52)（等 #51）；罗洁 [#53](https://github.com/pfx43/zhixu-backend/issues/53)；彭飞翔写功能更新第五节、书上展示学科。

**做完才轮到：** Tina 用 `gaps` 当派刷增强（D 之后，不是完成条件）。

---



## 3. 依赖关系（谁卡住谁）

```
积木：登录、存书、按页、出题、刷题、聊天检索
                │
        ┌───────┴───────┐
        ▼               ▼
   A 目标记住      B 段落带页码+目录
        │               │
        └───────┬───────┘
                ▼
           C 今日任务+检查器
                ▼
           D Tina 派任务
                ▼
           E 设计稿接 API

B 的目录提取 ──► F 书 domain → 出题锁 tag → 交卷 predict
                     （第二队列；等对面 Key/register 再开生产开关）
```

A 和 B 可以两人同时做。C 不要在 A、B 没合入时硬写死假数据当正式方案。F 不要挡 A–E，也不要和未合入的生产 bug 抢同一人。

---



## 4. 每人这一期碰什么、别碰什么

按人拆开的说明：[罗洁.md](./罗洁.md)、[张子麟.md](./张子麟.md)、[陈勇搏.md](./陈勇搏.md)、[彭飞翔.md](./彭飞翔.md)。


| 人   | 认领                                     | 别做                                |
| --- | -------------------------------------- | --------------------------------- |
| 罗洁  | A 目标表、引导写入、删号级联；F 等 TCN register 后建档 | 不要改 Tina 工具、不要做向量、不要改前端           |
| 张子麟 | B 切段写页码、章节目录、旧书补页；F 目录提取写 `tcn_domain` | 不要做向量化/外置向量服务、不要改前端、不要调 predict |
| 陈勇搏 | 出题页/邻页后端；C 任务表 + 检查器；C 通了做 D；F 出题锁 tag + 交卷 predict | 不要用 suggestions 冒充任务；不要勾选完成接口；不要改前端；不要接 TCN Learning OS |
| 彭飞翔 | 改前端（设计稿接 API）；整理 [功能更新.md](./功能更新.md)（含 TCN 第五节） | 不要改后端 Agent / 检查器 / 迁移            |


派活的人是王晨：只验收、拍规则（页码不许猜、不能勾选、不向量化）。不要把自己写进认领表，也不要去整理功能更新正文（那是彭飞翔）。

大家共同：查询必须带当前用户；完成弹窗文案不要改。

---



## 5. 不要做的

- 向量化、向量数据库、上传就整本出题
- 任务日历、任务地图、用户打勾、成就墙
- 用建议接口或针对训练冒充今日任务
- 用 TCN Learning OS 冒充知序目标/日任务；让模型自造知识点 tag 或节点 id



## 功能更新

下面是「为什么要做、做成什么样」。认领顺序仍按前面 A→B→C→D，tip 接现有笔记接口。TCN 是功能 F，细则 [TCN集成.md](./TCN集成.md)。不要另做日程、日历、勾选完成。

### 一. 目标和任务系统

#### 为什么会有

日程太重，也不好设计。程序现在缺两样东西：Agent 凭什么判断今天该干什么；用户打开软件时，怎样才算「在用对的方式推进」。所以加目标和任务。目标是长期那一句；任务是当天几件清楚的事，不是把一天排满的日程。

#### 实现

**目标：** 用户想靠这个程序达到什么，例如「研究生上岸」。字段：描述（那句话）、属性（科目 / 对着哪类资料等）、有效期。一人同时只强调一条进行中的目标。到期或结业靠测量（题、tag），不是用户说「我学会了」。

**任务：** 每天 1～3 件可跳转的事，用来推进目标。例如看某章、出某几页的题、刷指定题号。Tina / 任务 Agent 派，**程序判定完成，用户不能勾选**。完成时弹窗固定：「该任务已经完成！」 任务写进同一张 `daily_tasks`，不要每人一套表、也不要对话里另写一套。

派的顺序：没书 → 上传；有书没题 → 按章出题；有题 → 按章或按缺口刷。前置：[目录和段页码](#二-书本目录提取段上带页码) 和 [Agent 升级](#三-agent升级)。

这一期三类任务，**都能自动确认**：不是用户勾选，是检查器挂在已有成功路径后面，看见学习动作的结果。

1. **上传资料**  
   资料不够时派。点去完成：进资料页上传，或在 Tina 对话里弹出上传窗口。两条路都走现有上传接口，不是新通道。  
   为什么能自动完成：文件一旦进库，这个用户的学习区就多了一本书。检查器看见「多了能解析的文档」就标完成，和交卷、按页出题看见结果是同一类事。聊该传什么是对话的事；传没传成功是检查器的事。不要另做「我传完了」接口。  
   传的不是任务里说的那本：这一期仍然算完成。检查器不读文件名、不对内容做语义匹配（会猜错，也会把人卡住）。对不上目标是缺口，不是「没完成」。下一次 `ensure` / Tina 再看目标和书单，缺的还在就再派上传或让 Tina 在对话里说明还缺什么。空文件、解析失败、按 hash 去重没有新书：不算多了文档，任务继续挂着。

2. **按页出题**  
   有书没题、或某章还没题时派。payload 里是书 id + 页码（页码来自目录，禁止模型手填）。  
   点去完成：进**出题页**（设计稿 `generate.html`， 同一套三栏：左按章勾页、中预览原文、右「AI 批量出题」+ 每页进度）。任务跳转要带上书和预勾选页（设计稿 `?from=tina`），人可以改勾选。Tina 对话里出题也行，不要在聊天里另开一个出题 Agent。  
   两条路都走按页出题。出题页不要只打一次 `generate-from-pages` 干等，或后台跑、但必须把页每页进度推给出题页。不要把内部交题工具挂到对话 Tina。  
   页列表要能显示「已出题 N」（知序现在没有，因为题目没存页码）。勾选上限跟 `max_pages_per_gen` 走，设计稿是 10。  
   为什么能自动完成：那些页上已经有题。检查器查 payload 页的题数，不要看 HTTP 刚返回的 `processing`，也不等整本 `question_gen_status=completed`。  
   人改勾选、只出了别的页：payload 仍空着，任务继续挂着。多出了别的页、但 payload 里的页已经有题，算完成。检查器不对「是不是这一章」做语义判断。

3. **刷题**  
   上线时看目标和题库，从某章或「没做 / 错 / 不会」里抽出题号写入任务。点去完成：刷题页用这些题目 id 开会话。**完成条件：** 任务范围内交够约定道数。交了就算（含「不会」），不要要求全对，否则点「我不会」会把任务卡死。

不要做勾选完成的 API。上传 / 出题 / 交卷成功后，检查器返回 `completed_tasks`，空就不弹窗。

### 二. 书本目录提取、段上带页码

#### 为什么会有

出题已经能按页，刷题却还像随机抽。用户没法按章刷、按章复习。任务要「出某章的页」「刷某章」，也必须有目录。

另外：Tina 检索命中的是**段落**，出题接口要的是**页码**。段上没有页码，检索到了也无法出题，只能猜页——不许猜。所以**抽出目录**和**段上带页码**是两件事，都要做。不是向量化。

#### 实现

上传、解析、切段照旧。之后多两步（后台即可）：

1. **抽出目录（章 → 页）存进这本书**，前端出题页按章分组勾选（设计稿左侧已经是这样）。优先用书里已有的目录 / 标题 / 书签对页码。不要只把前几页丢给 Agent（知拾 learning_path 读前 8 页，且**没有页范围**，出题页只能扁平「第 N 页」）。Agent 只能作补充，不能现场编页码。
2. **切段时给每段写下 `page_start` / `page_end`**（一段可以跨页）。关键词检索命中原样带上书 id 和这些页。旧书要补一次，不能只对新上传生效。

按章出题、按章刷、检索后再出题：都是程序拿目录或命中上的页码，调用现有按页出题。不要把搜到的那段话丢给另一个 Agent 再猜页。

### 三. Agent 升级

#### 为什么会有

现在对话里的 Tina 几乎只会检索；知序出题 Agent 还不能看邻页（知拾已经能）；没有专门布置任务的 Agent。目标和按章学习接不上。

下面「Tina 框架」= 我们用的 Agent 库；「对话 Tina」= 用户聊天那个。新工具都用框架来挂。任务 Agent 的「布置」必须写入第一节那张 `daily_tasks`，不要另写一套。

功能 D 表里是最小必做（读目标、看目录、按页出题、看缺口、写入今日任务）。下面是对话里还要挂哪些、怎么展示，两边同一套积木，不要做两套任务。

#### 实现

身份只来自登录。工具参数里不要 `user_id`。文档 id / 题目 id 都要再验是不是这个用户的。

##### 1. 对话 Tina

提示词里带上：这个用户有哪些资料（没有就写无）、当前目标、任务 Agent 已经布置的今日任务。

工具：

- **检索知识库：** 可选资料 id（没有就在自己的库里搜），加关键字、top_k。返回片段必须带书名、文档 id、页码（来自第二节入库字段，不是模型填的）。没有页码就等于工具废了。
- **资料详情：** 用资料 id 取名称、按目录的题目数、知识点 tag 及数量。
- **按目录取题：** 书 id、目录名、类型（做过 / 错 / 不会 / 没做）→ 题目 id 列表。
- **按知识点 tag 取题：** 书 id、题的知识点 tag、类型 → 题目 id 列表。
- **展示题目：** 聊天里嵌一块可作答的题（SSE 块 / 前端组件），参数是题目 id。不是新出题通道，也不要整页跳走才算展示。
- **上传资料：** 对话里弹出上传窗口，走现有上传；成功与否由检查器看学习区是否多了书。
- **读 / 改目标、布置今日任务：** 走功能 D 那几个入口，写入同一张表。

不要把出题内部的 `submit_question` 挂到对话 Tina 上。

##### 2. 任务 Agent

提示词里带上：资料列表、各书章节和题量、最近任务完成情况、当前目标和有效期。

工具：按目录取题、按知识点 tag 取题（同上）、**任务布置**（写入 `daily_tasks`。kind / 完成规则由程序定，Tina 只写标题和理由）。

##### 3. 出题 Agent（内部，不是对话 Tina）

工作单位是**页**：出题页或 Tina 工具交出 `document_id` + 页码，服务按页取出正文，**每一页新建一个 Agent**（用完即丢），不跟用户聊天。页码由服务传入，Agent 不能自己改出哪些页。

知序现在只有一个 `submit_question`，正文截断后看不见上一页。

不要把这个 Agent 挂进主对话。

##### 4. 辅导 Agent

仍跟刷题会话走。进入新题时，提示词换成这题的题干、答案、解析。工具这一期先不扩。

### 四. 特色功能 tip

#### 为什么会有

笔记是用户自己沉淀下来的长内容（自己写、AI 摘要、学习报告）。

tip 不是第二种笔记首页。它是用软件时随手留下的短卡片：解析里一句、Tina 说的一句、资料里划到的一句，觉得以后开刷前能翻一翻，就收进来。给「过最近几张」，不是档案柜。

所以笔记页做成两个 tab：笔记 | tip。不要混成一个列表。

#### 长什么样（设计稿已经能点）

1. **划选触发**  
   在 Tina 对话、刷题辅导里，划一段字（大约 4 个字以上）。选区旁边冒出一个绿色小按钮，写着 **tip**。点侧栏、输入框里的字不会出这个按钮。

2. **收入弹层**  
   点 tip 打开「收入 tip」：上面是划选原文（引用块），下面填标题（可改，默认截一句）、关联哪本资料（可以不关联）、**tag**（用户自己给这张 tip 分类）。取消或点遮罩关掉。点「收入 tip」后进笔记页的 tip 堆，新的在最上面。

   tag 不是书或题目上的知识点。那是用户觉得这张卡片属于哪一类，例如「英语难词」「易错公式」「下次开刷前要看」。同一句原文，不同人可以打不同的 tip 类型。设计稿里还有一排「主题」chip，和 tag 重复的话只保留用户自己的类型，不要和资料 / 出题的知识点 tag 混用一张表。

3. **笔记页的 tip 堆**  
   默认几张牌叠在一起，左边一条鼠尾草绿。点最上面一张会抽到最底下（下一张也行）。「全部展开」铺开；再点其中一张会重新叠回去。每张牌：角标 tip、标题、引用原文、关联的书、从哪划的（Tina 对话 / 刷题辅导 / 资料阅读）。点开是详情，可以补一句「为什么留下它」。返回走 tip 堆，不是笔记列表。

4. **对话里也能看见、也能让 Tina 做**  
   用户说「把跟 build 有关的 tip 给我看」，Tina 在聊天里摊出相关的几张小卡片，整摞仍在笔记页。用户说「把刚才那句做成 tip」，Tina 做好后聊天里出现一张新 tip，链到 tip 堆。AI 侧仍然平铺正文，不要头像。

右侧今日状态里可以出现「新 tip」条数，不是独立功能入口。

#### 实现（给后端同学）

划选和堆叠 UI 在前端。后端不要为 tip 另做一套表。

- 落库走现有笔记接口，`note_type` 用 tip（和 `manual` / `report` 分开）。标题、正文（划选原文）、关联资料（`collection_id` 或文档 id，可空）用笔记已有字段。来源（Tina / 刷题 / 资料）写进正文或扩展字段，列表要能筛出来。
- **tip 的 tag**：存的是用户给这张短卡片打的类型（难词、易错点……），按当前用户隔离。列出、筛选、Tina「把某类 tip 拿出来」都走这套 tag，不要拿题目 / 文档的知识点 tag 来冒充。用户没打 tag 也可以收。
- 创建必须带当前登录用户。列表：`note_type=tip`，新的在前。删号时跟笔记一起删。
- Tina 读 tip、代做 tip：只动这个用户的 tip。代做时标题和关联资料由程序填，不要让模型编文档 id；tag 能从用户这句话里带上的才写，不要替用户乱分类。
- 设计稿现在写在浏览器 `sessionStorage`，接真接口后换掉。这一期不要做成任务，也不要日历。

站在积木「笔记」上。堆叠怎么翻是前端的事；后端负责存、按用户隔离、能按 type 列出。

### 五. TCN 知识点

见 [功能更新.md](./功能更新.md) 第五节和 [TCN集成.md](./TCN集成.md)。目录提取写学科；出题锁词表；交卷才 `predict`。「不会」不传。不接 Learning OS。

---

## bug 修复

不是新功能。修的时候不要顺手改产品口径。

1. **TCN 链接**  
   配 `TCN_BASE_URL`（或等价配置），不要把整站请求打到错误地址、更不要因此把站点打挂。链接失败要降级，不要拖死主流程，这个和会议说的一样，在王晨调查完之后再修改

2. **关键词检索漏检**  
   现在 `LIMIT 200` 且没有按相关度排序时，命中可能排在后面被丢掉。检索结果要能按匹配排序再截断；返回时带上段上的页码（见功能 B），没有页码不要让工具去猜。

3. **SSE 字段不要一锅端**  
   按 `type` 组字段。不要给所有事件都塞空的 `content`。展示题目、上传窗口这类块走自己的 type，前端才知道是嵌组件还是纯文本。

4. **聊天热路径**  
   主对话已经走 `llm_pool`。不要把默认路径退回 `TinaGateway` 的 key lease 旧写法。
