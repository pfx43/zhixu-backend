# TCN 算法方对接确认清单

> **文档目的**：与 TCN 算法团队对接前，逐项确认接口格式、字段命名、响应结构等关键信息，确保我方接入零报错。  
> **基准文档**：`SYSTEM_ARCHITECTURE_BRIEF.md`（v1.1, 2026-07-13）  
> **使用方式**：将本文档发送给 TCN 方，请对方逐项填写"实际值"列。

---

## 一、已提供接口 — 响应格式确认

TCN 文档已声明这 4 个接口，但仅 `predict` 请求体给出了 JSON 示例，其余接口的响应格式均为文字描述，**需要对方提供真实 JSON 示例**。

### 1.1 `GET /v1/user/report/{user_hash}` — 🔴 最高优先级

**TCN 文档描述**：返回所有节点掌握度（0–1）+ 每节点先修父节点掌握状态

**需确认**：

| # | 确认项 | 我方推测 | 实际值（请 TCN 方填写） | 备注 |
|---|--------|---------|----------------------|------|
| 1 | 顶层数据类型 | `array`（列表） | | 如果是 `object` 嵌套结构，我方解析逻辑完全不同 |
| 2 | 一个完整的 JSON 响应示例 | 见下方推测 | | **请直接贴真实 curl 响应** |

**我方推测的格式**（可能不对，请 TCN 方纠正）：
```json
[
  {
    "node_id": "skill_0",
    "mastery": 0.85,
    "prerequisites": [
      {"node_id": "skill_1", "mastery": 0.92}
    ]
  }
]
```

**请 TCN 方在此处粘贴真实响应**：
```json

```

---

**逐字段确认**（如果 report 是数组，以下字段是否存在于每个元素中）：

| # | 字段名 | 类型 | 我方需要 | 是否存在 | 实际字段名（如不同） |
|---|--------|------|---------|---------|-------------------|
| 3 | 节点唯一标识 | string | ✅ 必须 | | |
| 4 | 节点掌握度 | float (0–1) | ✅ 必须 | | |
| 5 | 节点中文名称（如"一元二次方程"） | string | ✅ 必须（前端展示用） | | |
| 6 | 节点 index 编号 | int | 🟡 需要（图谱排序） | | |
| 7 | 节点可信度 (confidence) | float (0–1) | 🟡 需要（图谱可信度圆圈） | | |
| 8 | 先修父节点列表 | array | ✅ 必须 | | |
| 9 | 父节点中是否包含其 mastery？ | — | 🟡 需要 | 是 / 否 | |
| 10 | 是否有单独的"后继节点"字段？ | — | 🟡 需要 | 是 / 否 | 如果没有，我方需遍历所有节点反向重建，503 节点 O(n²) 可行但慢 |

---

### 1.2 `POST /v1/user/predict` — 🟡 中优先级

**TCN 文档已提供请求体示例**（user_hash, current_node, user_action, domain_id, step_index, session_id），✅ 我方已按此实现。

**需确认**：响应中除 `node_mastery`、`lvr`、`vs` 外，还有哪些字段？

| # | 确认项 | 我方现状 | 实际值（请 TCN 方填写） |
|---|--------|---------|----------------------|
| 11 | 完整的响应 JSON 示例 | 只定义了 node_mastery/lvr/vs 三个字段 | **请贴真实 curl 响应** |

**请 TCN 方在此处粘贴 `POST /v1/user/predict` 的真实响应**：
```json

```

**补充确认**：

| # | 确认项 | 问题 |
|---|--------|------|
| 12 | `node_mastery` 是本次更新**后**的掌握度，还是变化值(delta)？ | 我方当前按"更新后"处理 |
| 13 | `user_action` 枚举值是什么？ | 文档写 `"correct"`，我方按 `"correct"/"incorrect"` 处理，是否有其他值？ |
| 14 | 新用户首次调用 predict 时是否需要预先初始化？ | 文档提到"初始掌握度默认 0.5"，是否直接调即可？ |

---

### 1.3 `GET /v1/user/profile/{user_hash}` — 🟢 低优先级

| # | 确认项 | 我方现状 | 实际值（请 TCN 方填写） |
|---|--------|---------|----------------------|
| 15 | 完整响应 JSON 示例 | 按 {global_lvr, total_steps, tracked_nodes_count} 解析 | **请贴真实 curl 响应** |

**请 TCN 方在此处粘贴**：
```json

```

---

### 1.4 `GET /health` — 🟢 低优先级

| # | 确认项 | 我方现状 | 实际值（请 TCN 方填写） |
|---|--------|---------|----------------------|
| 16 | 完整响应 JSON 示例 | 读 `status` 和 `skills_count` 字段 | **请贴真实 curl 响应** |
| 17 | 节点数字段名 | `skills_count` | |
| 18 | 是否返回图版本号？ | 未使用，但可记录日志 | |

---

## 二、需要 TCN 新增的接口 — 规格确认

以下接口 TCN 文档已列为"需要新增"，我方也急需使用。请确认：

### 2.1 `GET /v1/user/summary/{user_hash}` — P0 必须

**用途**：每次 Chat 对话前注入 LLM System Prompt，无此接口则三种模式无法区分。

| # | 确认项 | 问题 |
|---|--------|------|
| 19 | 预计可交付时间？ | |
| 20 | 是否包含以下字段？<br>— 已掌握节点数<br>— global_lvr<br>— 最薄弱领域（如"二次函数"）<br>— 最活跃节点<br>— 最近更新时间 | 圈出能提供的字段 |

---

### 2.2 `GET /v1/user/gaps/{user_hash}` — P1

**用途**：先修提示、断层诊断、错题归因。

| # | 确认项 | 问题 |
|---|--------|------|
| 21 | 预计可交付时间？ | |
| 22 | 返回格式是按严重度排序的节点列表？ | 我方期望：[{node_id, mastery, severity, missing_prerequisites}] |
| 23 | "严重度"的计算口径？ | LVR 贡献值 or 掌握度差值？ |

---

### 2.3 `GET /v1/user/vulnerabilities/{user_hash}` — P1

**用途**：推理模式 "伪掌握" 预警。

| # | 确认项 | 问题 |
|---|--------|------|
| 24 | 预计可交付时间？ | |
| 25 | 返回格式是"高掌握度 + 高 LVR 贡献"的节点列表？ | |

---

### 2.4 `GET /v1/user/lvr_alert/{user_hash}` — P1

**用途**：前端 LVR 预警 Banner + Chat 降级判断。

| # | 确认项 | 问题 |
|---|--------|------|
| 26 | 预计可交付时间？ | |
| 27 | 预警级别分级标准？（正常/注意/警告） | 我方暂定：0–0.15 正常, 0.15–0.35 注意, >0.35 警告 |
| 28 | 是否包含违反最严重的前 N 条先修边？ | |

---

### 2.5 图谱结构查询接口 — P2 建议新增

| # | 确认项 | 问题 |
|---|--------|------|
| 29 | 是否可提供 `GET /v1/graph/full` 返回全量节点+边（不含用户状态）？ | 有此接口可避免我方每次做 O(n²) 遍历 |
| 30 | 或 `GET /v1/graph/node/{node_id}` 查询单节点先修/后继？ | |

---

## 三、通用对接问题

| # | 确认项 | 问题 |
|---|--------|------|
| 31 | TCN 服务的基础 URL 和端口？ | 我方默认 `http://127.0.0.1:8001`，是否正确？ |
| 32 | TCN 是否需要鉴权（API Key / Token）？ | 文档未提及，我方按无需鉴权处理 |
| 33 | 是否有速率限制（rate limit）？ | 我方默认最多 2 次重试，每次间隔 1s |
| 34 | `user_hash` 的格式约束？ | 我方生成 sha256(uid+salt) 前 32 位 hex，TCN 是否接受？最大长度限制？ |
| 35 | 是否有 Swagger / OpenAPI 文档地址？ | 如有可直接导入，无需逐字段确认 |
| 36 | report 接口的节点总数上限？ | 文档提过 503，当前是否仍是 503？ |

---

## 四、优先行动建议

| 优先级 | 行动 | 不确认的后果 |
|--------|------|------------|
| **P0 - 立即** | 索要 `GET /v1/user/report/{user_hash}` 真实 JSON 响应 | 4 个改造接口全部无法正确解析，功能崩溃 |
| **P0 - 立即** | 确认 report 是否含 `name` 字段（中文名称） | 前端图谱显示 `skill_0` 而非"加法"，用户体验差 |
| **P1 - 本周** | 确认 4 个待新增接口（summary/gaps/vulnerabilities/lvr_alert）的交付时间 | 影响 Chat 三种模式、推理模式的开发排期 |
| **P1 - 本周** | 确认 Swagger 文档地址 | 一次性解决所有字段确认 |
| **P2 - 可延后** | predict/profile/health 的详细字段 | 不影响核心功能，仅影响日志/监控精度 |

---

*本文档可拍照或直接发送给 TCN 算法方填写。建议优先索要 Swagger/OpenAPI 文档，可替代大部分逐字段确认工作。*