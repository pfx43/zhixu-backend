# 后端能力补齐清单

> 审核日期：2026-07-04
>
> 对照基准：`docs/4.API接口文档.md`
>
> 文档定位：本文件只记录 `docs/4.API接口文档.md` 尚未覆盖、或已经明确标注为“预留但后端尚未实现”的后端能力。已经在 4 号文档形成明确路径、请求体和响应体的能力，不在本文重复列入补齐范围。

## 1. 对照口径

纳入本文的条件：

1. `docs/4.API接口文档.md` 没有对应接口域、路径、请求体和响应体。
2. 4 号文档已有相近接口，但只覆盖算法计算或基础能力，尚未覆盖用户态持久数据、业务状态机或产品模式字段。
3. 4 号文档明确标注为预留契约，后端尚未实现。

不纳入本文的能力：

- 已在 4 号文档形成正式契约的账号认证、基础聊天、知识库上传/列表/状态/删除/预览/配置、首页建议、KT 基础算法、套餐、健康检查。
- 仅属于前端页面接入、loading/error/empty 状态或 UI 展示的问题。
- 当前个人端最小闭环之外的机构端、学校端、家长端、公益版完整能力。

## 2. P0：个人端最小闭环缺口

### 2.1 三层产品模式与权益

4 号文档现状：

- `Auth` 和 `Plan` 已有套餐、配额、升级、我的套餐等接口。
- 现有字段能表达 `plan_level`、配额和模型访问，但没有直接表达产品侧三层模式。

后端还需补齐：

1. 用户当前模式：`basic_normal`、`knowledge_tracking`、`reasoning`。
2. 用户可用模式列表和不可用原因。
3. 每个模式的能力开关，例如是否启用画像图谱、知识追踪、推理审查。
4. Chat、KT、画像等接口返回实际使用的模式、降级原因和升级提示。

### 2.2 Chat 模式、知识库范围与引用

4 号文档现状：

- `POST /api/v1/chat` 只定义 `session_id`、`content`、`stream`。
- SSE 数据只定义 `session_id`、`role`、`content`、`reasoning_content`、`tool_name`。

后端还需补齐：

1. Chat 请求模式参数，例如基础问答、普通模式、画像确认、知识追踪、推理模式。
2. 可选知识库范围、资料范围、文档集合或标签范围。
3. 回答引用信息：命中文档、引用片段、相似度、来源位置。
4. 当前回答是否使用画像、知识库、KT 或推理能力。
5. 权益不足或上下文不可用时的降级原因。

### 2.3 个人知识库检索与画像资料选择

4 号文档现状：

- `KB` 已有上传、文档列表、索引状态、删除、内容预览和配置。
- 未定义面向用户知识库的检索接口，也未定义资料是否参与画像构建的选择关系。

后端还需补齐：

1. 个人知识库检索接口，支持关键词、语义检索、标签或文档类型筛选。
2. 检索结果返回文档、片段、匹配原因和可引用信息。
3. 标记资料是否可用于画像构建。
4. 选择或取消选择画像构建资料。
5. 文档与画像节点、Chat 引用、学习记录之间的关联关系。

### 2.4 音频上传与转写

4 号文档现状：

- `POST /api/v1/kb/upload` 已支持文本类文档、PDF、Docx、HTML、CSV、JSON、Markdown 和图片 OCR。
- 文档中未列出音频格式，也没有音频转写状态。

后端还需补齐：

1. 音频上传格式和大小限制。
2. 音频转写任务状态，例如 `transcribing`、`transcribed`、`transcribe_error`。
3. 转写文本预览和入库结果。
4. 转写失败时的用户可理解错误。
5. 转写结果是否可用于画像构建和 Chat 引用。

### 2.5 画像构建任务

4 号文档现状：

- 未定义个人画像构建接口。

后端还需补齐：

1. 创建画像构建任务。
2. 绑定用于构建画像的资料范围。
3. 基于资料生成确认问题。
4. 保存用户回答、否认和修正结果。
5. 输出画像节点、边、可信度和依据来源。
6. 在资料不足时返回待确认问题，而不是生成确定画像。

### 2.6 个人画像图谱

4 号文档现状：

- `GET /api/v1/kt/skill-graph` 是知识追踪技能图谱，不是个人画像图谱。
- 4 号文档未定义画像节点、画像边、可信度来源和用户修正接口。

后端还需补齐：

1. 获取个人画像图谱。
2. 获取画像节点详情。
3. 返回节点可信度、依据资料、最近确认时间和用户确认状态。
4. 提交节点不准确反馈。
5. 提交节点修正并重新计算可信度。
6. 修正后生成再次确认问题。

### 2.7 普通模式先修提示

4 号文档现状：

- `POST /api/v1/kt/prerequisites` 需要传入明确 `skill_id`。
- 未定义“从用户问题识别知识点并返回非阻断先修提示”的接口。

后端还需补齐：

1. 根据用户问题识别相关知识点。
2. 查询可用先修关系。
3. 返回非阻断提示：可能缺少哪些前置知识、为什么建议先补、没有可靠先修图时如何降级。
4. 与 Chat 普通模式联动，避免把先修不足变成硬拦截。

### 2.8 本地优先与一次性云端迁移

4 号文档现状：

- 已说明文件存储路径和可切换 OSS。
- 未定义换机迁移、下载回本地、云端副本清除等接口。

后端还需补齐：

1. 创建一次性云端迁移任务。
2. 查询迁移任务状态。
3. 下载迁移数据包。
4. 下载确认后清除云端副本。
5. 迁移任务过期、撤销和失败处理。

## 3. P1：知识追踪与现有页面入口缺口

### 3.1 用户持久化认知状态

4 号文档现状：

- `POST /api/v1/kt/correct`、`evaluate`、`learning-path` 都由前端传入 `states`。
- 文档未定义读取、保存和追踪用户真实认知状态的接口。

后端还需补齐：

1. 获取用户当前学科知识节点状态。
2. 保存节点掌握度、可信度、更新时间和变化原因。
3. 区分用户真实状态、算法临时输入和前端演示数据。
4. 支持状态修正后的持久化。

### 3.2 带用户状态的知识追踪图谱

4 号文档现状：

- `GET /api/v1/kt/skill-graph` 只返回 `skills`、`edges`、数量。
- 未返回用户掌握状态、可信度、布局或层级。

后端还需补齐：

1. 学科知识图谱叠加用户掌握状态。
2. 节点可信度和最近更新时间。
3. 可视化所需层级、分组或布局信息。
4. 从图谱节点追溯到相关资料、题目或学习行为。

### 3.3 先修断层定位

4 号文档现状：

- 已有技能先修/后继关系查询。
- 未定义“用户出错或提问后定位真正缺失先修节点”的诊断接口。

后端还需补齐：

1. 输入题目、错误回答、用户提问或学习事件。
2. 返回断层节点、断层原因和追溯路径。
3. 说明当前问题如何依赖缺失节点。
4. 给出下一步修复建议。

### 3.4 学习路径任务生命周期

4 号文档现状：

- `POST /api/v1/kt/learning-path` 只返回推荐列表。
- 未定义路径任务的开始、完成、跳过、重排和历史记录。

后端还需补齐：

1. 创建学习路径。
2. 标记步骤开始、完成、跳过、推迟。
3. 手动或自动重排路径。
4. 保存每一步推荐原因和完成结果。
5. 与用户认知状态更新联动。

### 3.5 错题归因拆解

4 号文档现状：

- 未定义错题或错误回答归因接口。

后端还需补齐：

1. 提交错题、错误回答或解析过程。
2. 归因到具体知识节点和先修缺口。
3. 返回为什么会错、从哪里断掉。
4. 将归因结果写入用户学习历史或认知状态。

### 3.6 LVR 实时监控与学习历史

4 号文档现状：

- `POST /api/v1/kt/evaluate` 可对前端传入的 `states` 返回 `lvr`。
- 未定义用户学习路径的 LVR 监控、变化曲线和活动历史。

后端还需补齐：

1. 查询当前学习路径的 LVR。
2. 返回 LVR 变化趋势和影响节点。
3. 记录学习行为、节点变化和阶段摘要。
4. 支持学习分析页面读取真实曲线和活动流。

### 3.7 笔记系统

4 号文档现状：

- 未定义笔记接口。

后端还需补齐：

1. 笔记 CRUD。
2. 搜索、排序、筛选和分页。
3. 标签、分类和关联资料。
4. 自动保存。
5. 从 AI 回答、资料片段或学习事件生成笔记。

### 3.8 提醒系统

4 号文档现状：

- 未定义提醒接口。

后端还需补齐：

1. 提醒 CRUD。
2. 重复规则、完成状态、延后和关闭。
3. 关联资料、知识节点、学习路径或笔记。
4. 到期事件与通知中心联动。

### 3.9 通知中心真实实现

4 号文档现状：

- `Notifications` 章节明确为预留接口契约，后端尚未实现。

后端还需补齐：

1. 通知落库。
2. 分页、分类、未读筛选。
3. 单条已读和全部已读。
4. 未读数量。
5. 与提醒、系统事件、知识库处理结果联动。

### 3.10 顶栏统一搜索与快速创建

4 号文档现状：

- 未定义统一搜索或快速创建接口。

后端还需补齐：

1. 统一搜索接口，至少覆盖知识库文档、资料片段、笔记、标签、知识节点。
2. 搜索结果类型、跳转地址和高亮摘要。
3. 当前用户可创建资源类型和权限。
4. 快速创建笔记、提醒、画像任务或上传任务的入口契约。

## 4. P2：推理模式缺口

4 号文档现状：

- Chat SSE 可返回 `reasoning_content`，但这只是推理文本片段。
- 未定义 `Dynamic-e` / `CABR` 驱动的推理模式、风险审查和可解释诊断接口。

后端还需补齐：

1. 高置信风险识别：返回回答置信度、逻辑审查结果和风险等级。
2. 多步推理链验证：返回推理步骤、每步校验结果和失败步骤。
3. 认知脆弱点主动暴露：发现高置信但脆弱掌握的节点，并给出干预建议。
4. 跨域推理支持：判断是否有可靠先修图；没有时降级为相关性引导。
5. 推理过程可解释：说明为什么需要先掌握某节点，以及当前判断依据。
6. 权益控制：推理模式不可用时返回降级原因和升级提示。

## 5. 建议补齐顺序

1. 先补三层模式与权益字段，避免前端继续硬编码产品模式。
2. 补 Chat 模式、知识库范围和引用，让基础问答、普通模式和画像确认能分开。
3. 补个人知识库检索和画像资料选择，打通画像构建输入。
4. 补画像构建任务和个人画像图谱，完成个人端最小闭环。
5. 补持久化认知状态、断层定位、学习路径生命周期和学习历史。
6. 补笔记、提醒、通知、统一搜索等页面入口真实后端。
7. 最后补推理模式的风险审查、推理链验证和可解释过程。

## 6. 本轮未验证项

- 未重新拉取远端 OpenAPI。
- 未检查后端仓库源码。
- 未验证线上后端活体状态。
- 本清单只基于 `docs/4.API接口文档.md` 与当前仓库文档口径判断“已覆盖/未覆盖”。
